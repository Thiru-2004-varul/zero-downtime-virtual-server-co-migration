apiVersion: v1
kind: Service
metadata:
  name: mobile-app-recreate-service
spec:
  type: NodePort
  selector:
    app: mobile-app-recreate
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30008
